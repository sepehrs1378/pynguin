import time


def memory_func(x: int) -> int:
    l = [i for i in range(100)]
    # time.sleep(0.02)
    # s = [i for i in range(10000)]
    # time.sleep(0.02)
    # t = [i for i in range(10000)]
    return 1
